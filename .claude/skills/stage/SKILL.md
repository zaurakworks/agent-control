---
name: stage
description: 按名取得某个工作阶段的完成判据与产出形状。仅在任务明确点名阶段（观察／提议／执行／判定）时按名调用；不要因为任务"看起来像"某个阶段就自动加载，也不要一次加载多个阶段。
---

# 工作阶段

四个阶段按**产出物类型**划分，不按工序。产出物类型决定谁消费它，以及"做完了"是什么意思。

| 阶段 | 产出物 | 消费者 | 一句话完成判据 |
| --- | --- | --- | --- |
| 观察 | 事实 | 下一阶段 | 未知项被列全，不是被回答 |
| 提议 | 选项 | **人** | 人能用一句话消费 |
| 执行 | 制品 | 判定或世界 | 自检通过，且没有自称验收 |
| 判定 | 接受／拒绝 | 门 | 能抓到种下的错误 |

## 怎么用

任务会说明本次是哪个阶段。**只读取对应的那一份 reference，不要读其他三份：**

- 观察 → [`references/observe.md`](references/observe.md)
- 提议 → [`references/propose.md`](references/propose.md)
- 执行 → [`references/build.md`](references/build.md)
- 判定 → [`references/verify.md`](references/verify.md)

任务没有说明阶段时**先问，不要自己猜一个**。猜错的代价是用错的完成判据交付，而那种错误在交付时看不出来。

## 运行面自述

**每个阶段的产出末尾都要有这四行**，逐字照抄标记名（格式是给机器读的）：

``SKILLS-AVAILABLE: 逗号分隔的全部可用 skill 名``
``SKILLS-LOADED: 逗号分隔的本次实际加载的``
``MCP-AVAILABLE: 逗号分隔的全部可用 MCP server／连接器名``
``CONTEXT-FILES: 逗号分隔的被自动加载为指令的文件绝对路径（CLAUDE.md、AGENTS.md 之类）``

- 确实为空写 `none`；看不到、判断不了写 `unknown`。**这两个不是同义词**——`none` 是观察结果，`unknown` 是观测失败。
- `CONTEXT-FILES` 只算运行时**自动**加载为指令的，本次任务自己去读的普通文件不算。
- 只报实际看得见的。为了填满而推断出来的清单比空清单更有害：它会让漂移检测报无漂移。

## 一个阶段一个进程

阶段之间只传制品，不传对话。上一阶段的推理过程物理上到不了下一阶段——这是设计，不是限制。

做完本阶段就停。不要在同一次运行里连做两个阶段，尤其不要自己执行完再自己验收。
