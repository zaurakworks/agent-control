# 任务：报告本次运行的实际配置状态

**本次是观察阶段。**

## 要采集的事实

1. **你当前可用的全部 skill 名字**（逐个列出，不要概括）。
2. **你实际使用的模型标识**。
3. **你的配置目录**（`CLAUDE_CONFIG_DIR` 环境变量的值，以及你实际从哪里读到 skill）。
4. **你能看到哪些 plugin**（有就列，没有就写"无"）。
5. **当前工作目录**。
6. **被运行时自动加载为指令的文件**：`CLAUDE.md` / `AGENTS.md` 之类，逐个给绝对路径。你自己去读的普通文件不算。
7. **你能看到哪些 MCP server 或连接器**（逐个列名，没有就写"无"）。

## 为什么问这个

这次运行的 home 是从一份 manifest 物化出来的，**声明只允许一个 skill（`stage`）**。本任务要核验声明态与实际生效态是否一致。

如果你看到白名单之外的 skill，那就是漂移——**如实报告，不要替它找理由。**

## 产出

写到 `.\out.md`，最后一行给出该文件的绝对路径。
