# 当前任务

> 状态：完成。  
> 授权：本次最小实施授权已经用完；后续工作需要新的明确授权。  
> 批准依据：负责人在 A 的最小实施决策点选择“1”。

## 向上追溯

- 总目标：[`../../authority/00-map.md`](../../authority/00-map.md)
- 首个 MVP：[`../../authority/06-first-mvp-direction.md`](../../authority/06-first-mvp-direction.md)
- 验证策略：[`../../authority/07-mvp-validation-strategy.md`](../../authority/07-mvp-validation-strategy.md)
- 实现方向：[`../../authority/08-mvp-implementation-direction.md`](../../authority/08-mvp-implementation-direction.md)

## 本次问题与期望结果

把已经确认的“版本化文件为中心、先人工运行”做成一个干净、可读取、可恢复的本地最小版本，同时避免引入旧仓内容和自动化复杂度。

## 已授权范围

- 创建本地 Git 仓库 `agent-control`；
- 原义迁移当前 9 份权威文档；
- 创建最小 `README.md`、`AGENTS.md`、`CLAUDE.md` 和本文件；
- 核验 Codex 与 Claude 的官方入口规则；
- 进行静态检查并创建本地 Git 提交；
- 最多使用一个新的只读 Codex 上下文检查恢复能力。

## 明确禁止

- 不创建或连接远程仓库；
- 不吸收旧仓、旧 Issue 或旧实现；
- 不修改全局 Codex、Claude 或 Agent 配置；
- 不创建 Skill、Hook、MCP、Plugin 或自动化；
- 不建设知识库、翻译规则或资源监控；
- 不进入多 Agent 并发工作。

## 验收条件

1. 初次迁移的 9 份权威文档与引导权威逐一哈希一致；此后只有总图和实现方向因记录新的明确实施授权而更新；
2. 新 Session 的共享读取路径只有权威总图、当前任务及当前任务明确列出的相关权威；
3. Codex 与 Claude 使用同一份最小入口规则；
4. 权威和任务状态能够明显区分；
5. 仓库没有远程地址，没有旧仓内容，没有全局配置修改；
6. 所有预期文件进入一个本地 Git 提交；
7. 新的只读 Codex 上下文能够说明目标、状态、授权边界和下一步；如果检查不可用，则明确保留为未验证。

## 当前进展

- 已从官方文档确认 Codex 读取仓库内 `AGENTS.md`；
- 已从官方文档确认 Claude Code 读取项目 `CLAUDE.md`，并支持用 `@AGENTS.md` 导入共同规则；
- 已创建干净的本地 Git 仓库；
- 已迁移并核对 9 份权威文档；
- 已创建最小入口和当前任务文件。
- 已完成静态检查：共 13 个文件，链接有效，无符号链接，无远程仓库；
- 已完成一次新的只读 Codex 恢复检查；它正确恢复了总目标、当前任务、授权范围、禁止事项和下一步；
- 恢复检查发现实施授权没有回写权威、实际执行顺序未及时更新等问题，现已修正；
- 为让首个 Git 提交包含完整的最终状态，恢复检查先于提交执行；这项顺序变化已明确记录。

## 关键决定、证据和未知

| 类型 | 内容 |
|---|---|
| 决定 | Claude 使用 `@AGENTS.md`，不复制一份共同规则 |
| 决定 | 首个 Git 提交应包含验收后的完整状态，因此只读恢复检查先于提交 |
| 证据 | OpenAI 与 Anthropic 官方文档都确认了对应的仓库入口行为 |
| 证据 | 新的只读 Codex 上下文正确读取 7 份必要文件，并恢复了目标、状态、边界和下一步 |
| 限制 | 只检查了一次短任务，不能证明长程工作不会漂移 |

## 下一步

当前没有进行中的任务。等待负责人决定下一项工作；不要自行恢复已暂停事项或开始知识库、自动化、多 Agent、远程仓库等工作。

## 验收结果

1. 初次迁移的 9 份权威文档哈希全部一致；新的实施授权已显式回写总图和实现方向；
2. 共享读取路径保持为权威总图、当前任务和当前任务列出的相关权威；
3. Codex 使用 `AGENTS.md`，Claude 使用 `CLAUDE.md` 导入同一份 `AGENTS.md`；
4. 权威与当前任务位于不同目录，状态清楚；
5. 仓库没有远程地址、旧仓内容、符号链接或全局配置修改；
6. 13 个预期文件已进入首个本地 Git 提交；
7. 一个新的只读 Codex 上下文正确恢复了目标、状态、授权边界和下一步，同时发现并促成了授权记录修正。

本次检查只提供初步正向证据，不能证明长程工作不会漂移。
