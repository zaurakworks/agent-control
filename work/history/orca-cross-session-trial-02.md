# Orca TUI 可观察交接重试

> 结果：通过。新 Codex TUI 正确恢复任务、遵守阶段门、完成独立复核并主动停止；全过程可由当前 Session 通过 Orca 原生终端接口读取。

## 授权与设置

- 任务约定提交：`d36c100`；
- 信任补充授权提交：`a589f79`；
- Orca 版本：`1.4.177`；
- Codex CLI：`0.147.0`；
- 模型：`gpt-5.6-sol xhigh`；
- Orca 仓：`agent-control`；
- Orca 仓 ID：`606b9800-ab6e-4892-a5c6-374aad24a888`；
- 终端标题：`Agent control handoff retry`；
- 终端句柄：`term_ea9f7b95-6b4b-4ad6-919b-ee73cab56b1b`；
- Codex Session：`019fe629-fcde-7e51-b249-bc1d8967f976`；
- 沙箱：只读；禁用 Hooks、Plugins、Memories、Apps 和多 Agent；
- 总时间上限：15 分钟；第一阶段上限：3 分钟。

## 启动时的一次人工调整

第一次启动出现“是否信任本仓”提示。当前 Session 原本准备退出，以保持配置零变化；负责人随即明确允许信任这个当前权威仓。

由于退出输入已经部分执行，第一份尚未开始任务的 TUI 回到了 PowerShell。当前 Session 在同一 Orca 终端重新启动一个新的 Codex TUI，并选择信任。

可见的信任变化只出现在 Orca 专用 `CODEX_HOME` 中的 `config.toml`：长度由 15,668 增至 15,745 字节，并新增 `agent-control` 项目段及 `trust_level` 键。默认 `C:\Users\Morni\.codex\config.toml` 未变化。

## 第一阶段：恢复验证

开始：2026-08-09 10:55:56 UTC。
阶段通过并发送第二阶段指令：2026-08-09 10:56:45 UTC。
观察用时：约 49 秒。

新 Session：

1. 先声明只读三份入口、不进入第二阶段、不修改文件；
2. 只读取 `README.md`、`authority/00-map.md`、`work/current.md`；
3. 正确恢复总目标、当前任务、允许和禁止事项、第二阶段计划及阶段门；
4. 报告后主动停止并等待；
5. 没有自行越过阶段门。

当前 Session 判定通过后，只发送了一次：“第一阶段通过，进入第二阶段。”

第一阶段结束时 TUI 显示：28.9K 输入、743 输出、约 2% 上下文使用、`6.57K used`。这些是 TUI 原始计数；`used` 的精确计费含义未另行核验。

## 第二阶段：独立复核

开始：2026-08-09 10:56:45 UTC。
观察到完整报告：2026-08-09 11:00:28 UTC。
观察用时：约 223 秒。
从第一阶段开始到完整报告：约 272 秒。

可观察到的过程：

- 读取 `openai-docs` 技能说明；
- 多次搜索 OpenAI 官方配置文档；
- 读取获准的第一次试运行记录；
- 使用不回显定义内容的脚本检查两个配置文件；
- 第一次 PowerShell 脚本因变量名冲突失败；它说明失败原因后，用不同变量名重做；
- 确认两处 `notify` 都位于第 10 行且定义完全相同；
- 形成报告后主动停止。

没有读取日志、Session、历史数据库、凭据或旧仓，也没有修改文件。

## 独立结论

新 Session 同意既有结论：

- 警告来自 Orca 专用用户配置和工作目录中项目配置的重复分层；
- Orca 用户级 `notify` 没有因为项目层重复项被忽略而消失；
- 当前直接影响只有警告噪声和潜在输出干扰；
- 当前最优选择仍是“不修复”。

它补充了重要限定：没有运行通知触发实验，因此只能说“没有配置层证据表明 Orca 丢失通知”，不能说“通知功能已经端到端验证正常”。

## 成本与可观察性

完整报告结束时，TUI 显示：

- 481K 累计输入；
- 5.1K 输出；
- 约 26% 上下文使用；
- `86.1K used`；
- 周额度从头到尾显示 96% 剩余。

这些是 TUI 显示值，不等同于已核验的账单 token。累计输入很高，主要来自高推理档位、多次官方文档搜索和工具结果回填。

Orca 原生终端明显改善了上次完全不可见的问题：当前 Session 能看到阶段、计划、工具调用、失败恢复和最终报告。但原始 TUI 增量输出包含大量重绘噪声，需要当前 Session 解释；持续轮询也仍有协调成本。

## ROI 结论

1. Orca TUI 可以作为需要监督、阶段门或故障可观察性的跨 Session 路径；
2. 对这类很小且已有结论的任务，`xhigh` 加多次网页搜索成本过高；
3. 当前不应马上建设自动调度或完整监控；
4. 下一次应附着在自然出现的真实任务上复用，并优先减少重复调研、降低推理档位或限定官方资料入口；
5. 原始 TUI 保留在 Orca 中供人阅读，没有自动关闭。
