# Issue #6 Plan：低频 / 事件触发上游回顾

## 人话结论

负责人选择低频 / 事件触发的手工回顾，并要求时间兜底。采用“事件先到先做，否则最迟 90 天”的规则：本次检查日为 `2026-08-08`，下一次最迟检查日为 `2026-11-06`。

不建设定时脚本、GitHub Actions、本机提醒或自动更新。用一个已分配、父子关系明确的 GitHub Issue 保存下一次检查的截止日、触发条件和输入，使它跨 Host 可见。

## 选定方向

- 事件触发条件：
  - 准备吸收第二个真实上游资产；
  - `grilling` 出现行为或 Provider 兼容问题；
  - 负责人主动要求检查。
- 时间兜底：若上述事件都没有发生，也要在 `2026-11-06` 前完成下一次三路径检查。
- 下一次仍先比较已采用的两个文件和根许可证；若资产集合在此之前经过批准发生变化，再以当时权威来源更新范围。
- 有变化只报告影响和建议，不自动修改版本、正文或安装状态。

## 为什么用 GitHub Issue 而不是提醒自动化

- 当前唯一样本为零变化，核心检查只花约 43 秒，月度执行系统没有足够 ROI 证据。
- GitHub Issue 已经是本项目跨 Host 的工作队列，可以保存负责人、父事项、前置、触发条件和截止日期。
- 标题和验收标准中的日期不是自动通知保证，但它让时间兜底成为可审查、可检索的开放工作，而不是只存在某台电脑或对话记忆中。
- 如果以后证明“开放 Issue 仍会被遗漏”，那是新的可观察证据，再比较 Milestone、Project due date 或定时提醒；本轮不提前建设。

## 有序任务

1. 在 Issue #6 记录负责人的选择、90 天计算和不自动化边界。
2. 创建一个父事项 #1 下的后续检查 Issue，分配给当前负责人；把 #6 记录为已完成前置。
3. 后续 Issue 明确截止 `2026-11-06`、三个事件触发条件、三路径默认范围、调研升级门和完成证据。
4. 验证后续 Issue 的负责人、父事项和前置关系。
5. 移除 Issue #6 的 `codex-pending`，将 #6 以 `COMPLETED` 关闭；保持父事项 #1 开放。

## 验证

- 后续 Issue 在 GitHub 可读，标题或正文含准确截止日 `2026-11-06`。
- 后续 Issue 分配给 `Eridanus117`，父事项为 #1，前置为 #6。
- 后续 Issue 不要求脚本、定时任务、自动更新或全仓扫描。
- #6 关闭原因为 `COMPLETED`；#1 仍为 `OPEN`。
- 仓库 `main` 不产生新提交，用户配置不发生变化。

## 风险与回退

- **Issue 可能仍被忽略**：当前先接受这个低成本机制；若实际错过截止，再用该证据比较更强提醒。
- **三个月内出现事件**：提前执行后可重新计算下一次 90 天兜底，不需要等到原截止日。
- **范围在三个月内变化**：只有已批准的新资产进入权威集合后才调整比较路径；不自动扩大全仓。
- **上游发生真实变化**：只生成影响分析并再次进入负责人决策，不自动吸收。

## 完成定义

- 负责人选择和时间兜底被持久记录；
- 一个可恢复、已分配、有截止日和触发条件的后续 Issue 存在且关系正确；
- 当前 D1 以完成关闭，父事项保持开放；
- 没有实现自动化或修改仓库资产。

